<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/iv_icon"
        android:layout_width="@dimen/text_size_px_100"
        android:layout_height="@dimen/text_size_px_100"
        android:layout_marginTop="@dimen/text_size_px_60"
        android:src="@drawable/news_icon_weixin"
        android:layout_centerHorizontal="true"/>

    <TextView
        android:id="@+id/tv_name"
        android:layout_width="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_height="wrap_content"
        android:textColor="@color/text_black"
        android:layout_below="@+id/iv_icon"
        android:layout_marginTop="@dimen/text_size_px_26"
        android:layout_marginBottom="@dimen/text_size_px_20"
        android:text="微信"
        android:textSize="12sp"/>

</RelativeLayout>